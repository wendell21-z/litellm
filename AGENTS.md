Read @CLAUDE.md for coding guidelines

Use the Ponytail skill at full intensity for every coding task in this project. Keep it active for the entire session unless the user explicitly says "stop ponytail" or "normal mode".

For work in `macos/LiteLLMProxyApp`, use the `build-macos-apps` plugin skills that match the task: `build-run-debug` for builds, launches, and runtime failures; `swiftpm-macos` for SwiftPM build and test work; `swiftui-patterns` for SwiftUI scenes and desktop UI; `appkit-interop` for necessary AppKit bridges; `window-management` for window behavior; `telemetry` for runtime event logging; `test-triage` for test failures; `liquid-glass` for Liquid Glass UI; `signing-entitlements` for signing or Gatekeeper issues; and `packaging-notarization` for distribution. Use only the narrowest applicable skill, and continue to apply Ponytail full.
